# Task 6: approved Room UI, with speaker editing on the first screen

**Current handoff instruction (2026-09-19): implementation is approved. Continue in `D:/DEV/_PERSONAL/FluidEQ` on `main`. The prior worktree isolation and no-commit instructions are superseded by the user's request to commit, push and merge everything into main. Preserve concurrent edits and follow the repository checks and normal commit/push workflow. Newer main commit eeea63a3a retired Ocean and renamed Black to Dark; use current main theme tokens and do not restore retired themes from these historical mockup examples.**

## Binding user requirement (overrides the mockup)

Keep the proposed interface, but speakers MUST move by hand on the FIRST Room screen. Clicking or beginning a drag selects that speaker and immediately shows its own angle, distance, level, mute and solo controls right beside/under the diagram on that same screen. Improve the current DspRoomSpeakerPanel. Do not send the user to Tune to edit a speaker. Keep the pane visible and updating while dragging. Preserve paired dragging and Shift/Ctrl unpaired dragging. Keep the advanced Tune/Fit interface for more complex settings.

The approved visual reference is C:/Users/ivanc/.codex/visualizations/2026/09/19/01a0ba24-bfc0-72d0-a269-4376df2bf665/fluid-room-preview.html. Reuse current FluidEQ components, Ocean/Black tokens, system font, SVG room/speaker visual vocabulary; no new icon/font/design dependencies. The reference's Adjust-to-Tune behavior is explicitly superseded by the paragraph above. Actual product source is the styling authority.

## Observed current layout risks

Controller built the real Room component in an isolated browser QA harness. At 1280x720 its selected speaker pane starts at y=909 because the diagram expands to 640px; individual SVG speakers have no keyboard role/tabindex. The upgrade must address both: compact diagram/pane composition and actual keyboard selection/editing. A graph role=img may hide interactive SVG descendants, so choose accessible semantics intentionally.

## Product behavior

1. Profile summary, Browse, source truth, Room power, Original/Room comparison, and Room/Tune/Fit navigation, styled like the approved proposal. Preserve ordinary previous/next/profile actions where useful and distinguish Restore profile from full Reset. Full Reset retains its exact all-Room-except-power meaning. Restore profile preserves listener/source preferences. After local edits mark Custom while remembering the chosen profile for a useful restore action; do not guess a profile after reopening an unknown Custom room.
2. Browse within the card: Featured, Classic, Saved. Six featured profiles from Task5; retain all eleven classics and saved rooms. Applying a built-in is one action, closes the browser, turns Room on, preserves head/correction/source preference/performance/other processors. Saved names must fit without overflow and saves must not silently overwrite another room (Task5 implements service behavior; expose it accurately).
3. Room view: compact top-down diagram, immediate selected-speaker pane, quick Space/Ambience/Distance, concise listener/headphone summary and source label. On desktop put the editor beside the diagram or compactly below; at narrow width stack without making speaker editing a different screen. Drag should select even when no click release occurs, and the pane should follow the active speaker. Ensure pointer capture/cancel safety, commit once on release, selection keyboard Enter/Space and useful keyboard angle edits; numeric/dial editing remains accessible. Do not drop paired drag or mute/solo behavior.
4. Tune: room size/walls, reflection/late decay/damping, centre/sub, bass management/crossover, stereo expansion amount and Preserve position. Reuse actual signal model, including source-fed/derived/unused speaker state. Global Distance changes the ring and own distances as before. Existing speaker-distance semantics must stay intact. Per-speaker essentials remain on first Room view even if also exposed under Tune.
5. Space maps monotonically to stored earlyReflectionDb with exact zero at -60; Ambience to ambienceMix. Use physical fields for persistence. Classic v1 ignores new controls, so do not show working-looking controls that do nothing: explicitly offer a user-triggered upgrade to new Room sound or keep v2-only controls disabled with a clear explanation until a featured profile is chosen. Never silently retune old saved rooms by merely viewing the card.
6. Fit: current head variants/manual correction, existing guided comparison dialog and tie/pair ordering. Explain truthfully that variants resize one measurement head, not three measured people or anatomical scanning. Manual head/correction stays free; guided Fit retains Plus.
7. Compare: wire actual Task4 Room-only delay-aligned comparison and honest level-match state. No fake level match, quality score or total-zero-latency promise. Label conventional stereo fold-down for discrete surround. Already-spatial source is an explicit manual bypass preference, never automatic detection. Show why Room/Dimension are bypassed where relevant using actual status/settings; do not invent engine state during missing/idle/unknown conditions.
8. Preserve entitlement boundaries: all built-in profiles, manual head/correction, existing bass management/crossover/upmix and mute/solo remain free. Manual shaping/speaker dragging/edits/saved editing/Guided Fit keep existing Plus checks. New manual shaping follows those checks. No new locks or account requirement.
9. All user-visible copy in all ten existing locales in the same change. Update stale current hints claiming bass/upmix are outside profiles; the existing implementation already saves/applies them. Do not introduce implementation jargon into the consumer flow. Reuse SCSS typography tokens; respect reduced motion; no setTimeout/setInterval/time-based UI fixes. Keep meter/status subscriptions isolated from the whole card/profile library.
10. Focused components/Room style partials where appropriate; do not grow DspRoomCard or Dsp.scss into another monolith. Avoid nested scrollers, clipping menus, hover-only meanings and inaccessible SVG interaction.

Source-state detail verified by controller: native room.cpp uses sub_gain for the actual LFE channel; the bass-management low_sum joins its bus separately at unity. The existing `music` UI state currently enables Sub even on a known two-channel stream, although its Sub level/mute cannot affect a nonexistent LFE input. Keep the existing native/legacy routing, and make the redesigned sub availability/text truthful for known stereo expansion versus a real surround LFE input. Do not infer a discrete LFE channel from stereo expansion. Unknown/idle remains unknown rather than a claim of measured silence. Cover front-stage and expanded stereo, real5.1/7.1 and unknown separately.

## Comparison/status interfaces prepared by Task4

Use the finalized Task4 report before coding; current exported interfaces are `IHostAnalysisRoom` (src/common/dsp/analysisWire.ts) with active, original, matchAvailable, referenceGainDb, conventionalFoldDown, positionProtected and sourceBypassed. Absence means unsupported/unavailable, never an inferred zero or ready state. `readDspRoomReport` and `subscribeDspRoomReport` in roomTelemetry.ts expose an isolated store. `readSystemDspChainResult` and `subscribeSystemDspChainResult` in systemChain.ts expose write results including update-required. A filesystem write result is not evidence the engine has processed audio. Keep status subscriptions in focused child components, not the whole Room card. The controller harness now has Native report and Engine capability fixture selects; these simulate reports at the IPC/report boundaries while importing actual stores and UI, and are labelled no audio.

## Keep audible dragging current

Controller traced live changes: applyDspSettings publishes every gesture update; system file writes already coalesce, but useNativeBackend calls controller.update for every React settings change and nativeBackend.ts serializes every captured snapshot into the global hostWork promise chain. Room preparation now measures about24ms for7.1 at48k and up to240ms at192k, so replaying every queued pointer position can leave the sound following a released pointer long afterwards. Address this required gesture behavior with completion-driven latest-value coalescing of adjacent pending settings updates (one in flight and one latest pending snapshot), preserving global engage/disengage/controller ordering and ensuring the final released value reaches the engine. No timers/debounce guesses; no discarded lifecycle barriers, stale-controller application, swallowed failures, or updates after release. Keep the public API truthful for coalesced callers. Add controlled-promise tests covering a blocked first update, many replacements, eventual final value, failure recovery, and a disengage/second-controller boundary. Do not broaden this into unrelated supervisor timeout work. Relevant paths: src/renderer/dsp/nativeBackend.ts, useNativeBackend.ts, src/**tests**/unit_tests/renderer/dspNativeBackend.test.ts and existing cross-controller lifecycle tests.

## Verification

- Meaningful Jest behavior: first-screen speaker select/drag/pane updates, paired/unpaired/cancel/keyboard, normal/no-Plus access, profile restore/full reset, source changes/solo release, unavailable centre/sub, classic/new coexistence, non-destructive save/restore, truthful compare/source-bypass states and locale keys. Existing tests may be updated for intentional UI navigation changes, never weakened to evade actual functionality.
- Run relevant Jest directly, typecheck, changed-file lint and style checks. Three baseline TS errors existed before this task; inspect current results rather than assuming still present.
- Controller owns actual browser QA using a real-component harness already available: artifact root above has build-room-ui.cjs, room-ui-harness.tsx, output room-ui/. Build with bundled Node. It substitutes only entitlement and source state, no audio; it imports actual Room components/styles. Do not mistake this for installed engine playback. You may run this webpack build to ensure it still bundles; coordinate with controller for browser interaction.
- Controller will inspect normal/narrow (including 375/320px), Ocean/Black, German text, keyboard, source states, Free/Plus, save/profile/compare flows and console logs. Prepare full implementation before reporting ready, then fix concrete review findings through controller.

## Deliverable

Write .superpowers/sdd/room-upgrade-plan/task-6-report.md with changes, exact checks/results and remaining concerns. Return short status/checks/concerns/report path. No commits; controller integrates and reviews scoped snapshots.
