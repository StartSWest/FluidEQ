# Task 5: six useful v2 profiles and non-destructive saved rooms

**Current handoff instruction (2026-09-19): implementation is approved. Continue in `D:/DEV/_PERSONAL/FluidEQ` on `main`. The prior worktree isolation and no-commit instructions are superseded by the user's request to commit, push and merge everything into main. Preserve concurrent edits and follow the repository checks and normal commit/push workflow. Newer main commit eeea63a3a retired Ocean and renamed Black to Dark; use current main theme tokens and do not restore retired themes from these historical mockup examples.**

## Scope

Add the approved six Featured profiles using Room rendererVersion=2: Reference, Music Space, Cinema (new distinct identifier), Game World, Competitive, Live Venue. Keep all eleven existing presets as Classic with exact IDs, order, values and legacy renderer behavior. Keep existing saved rooms readable and sounding the same. No new dependencies/data/SDK/THX names or obligations. No other rack processing changes, no global latency-mode mutation. UI redesign is Task6; provide a clean library API and localized descriptions now.

## Implement

1. Append six distinct IDs AFTER the existing custom entry in ROOM_PRESETS; every old numeric wire ID must retain its meaning. Extend the literal union, normalization/decoder validation metadata/native bounds and fixtures as necessary. Existing names like cinema/gaming/studio must not be repurposed. Prefer clearly versioned new IDs (e.g. referenceV2, musicSpaceV2, cinemaV2, gameWorldV2, competitiveV2, liveVenueV2). Export explicit Featured and Classic lists; preserve old grouping contracts for consumers until Task6 changes the browser. Avoid manually duplicated name/shape mappings.
2. Featured profiles own the entire room shape including bass/upmix, levels/distances/mutes and the v2 fields. They preserve personal head/headphone correction, sourceAlreadySpatial, transient compare selection, global performance/device settings and all other processors. Applying via the card still turns Room on. Head/correction remain free; do not expand any entitlement boundary.
3. Distinct conservative intents: Reference = restrained close front stage, Ambience off. Music Space = moderate width, gentle labelled stereo expansion, short/quiet ambience. Cinema = supported centre and bass with moderate damped ambience. Game World = discrete direction, short/quiet ambience, no implicit stereo expansion. Competitive = no early or late ambience, front stage for stereo, preserve discrete surround cues. Live Venue = longer damped tail, bounded expansion, restrained gains. All use preservePosition=true. Upmix only acts on stereo; never call derived signals real 7.1/object audio.
4. Choose final bounded numeric values using Tasks2/3 measurements and measured impulse/noise/headroom checks. Suggested starting values only (change on evidence): early dB -24/-12/-8/-16/-60/-6; ambience 0/.10/.18/.07/0/.25; decay .2/.3/.65/.25/.1/1.0; damping 8000/7000/5000/8000/10000/4500; ring distance 1.2/1.8/2.2/1.6/1.0/2.4; modest geometry inside room. Keep sub no higher than +1 dB initially. Do not compensate by manipulating unrelated processors or adding a hidden limiter. These are measured starting profiles pending real listening, not a claim of final preference superiority.
5. Save new rooms non-destructively. Existing case-insensitive same-name save silently replaces old shape/id; change this to append a new unique name (suffix such as ' (2)' within the 40-character cap), preserving all existing entries and IDs. Handle trimmed/case-insensitive duplicates and already suffixed names robustly; return the actual saved label for UI. Keep empty-name behavior safe. Update Room-only save-dialog wording to match without changing other DSP editors' overwrite semantics. Report storage failure honestly if the related save flow currently cannot distinguish success; do not advertise a persisted save when it failed.
6. Add profile label + concise purpose/source text in all TEN current locales in one batch. Product copy explains what the user hears/does; no DSP implementation claims or THX certification. The Task6 browser consumes these keys; send the API and key names to controller early. Existing saved-room names are user content, not translated.
7. Preserve explicit restore-profile versus full reset contracts for Task6. Classic to v2 is explicit, not an automatic migration. Persisted physical settings own the sound; missing rendererVersion still defaults to1, and known preset ID does not silently overwrite saved custom values.

## Evidence

- Targeted Jest: old preset numeric IDs/values unchanged, new IDs round-trip/clamp/wire, profile ownership vs listener/source/global settings preservation, edited built-in becomes Custom, saved v1/v2 coexistence, same-name/suffix/truncation cases preserve old rooms, empty/corrupt/unwritable storage handled without destructive behavior.
- Native profile fixtures mirror actual new table (prefer generation or a parity assertion to prevent divergence). Measure all six through real shipped head at four sample rates, stereo/5.1/7.1 and both modes where relevant; finite output, LFE/sub alignment, high-rate reflection presence, intended direct/late differences and modest peak/broadband coloration. A full-scale correlated multi-speaker downmix naturally needs upstream headroom; report actual peak/headroom rather than hiding it behind clipping. Reference/Competitive late path exactly off.
- Keep focused old preset/native tests. Controller has frozen v1 impulse banks for independent byte comparison. Do not weaken old expectations. Run relevant typecheck/lint (existing unrelated baseline failures recorded by controller), no dependency install or app/engine launch.
- Report actual measurements and any remaining listening/CPU limits; no 'sounds better' assertion without ears.

## Deliverable

Write .superpowers/sdd/room-upgrade-plan/task-5-report.md with final numeric profile table, persistence behavior/API, exact files, commands/results, peaks/energy/latency/CPU results and remaining listening limitations. Return short status/checks/concerns/report path. No commits. Coordinate native builds with controller.
